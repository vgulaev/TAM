var BPIElement = function ( argument ) {
	BPIElement.BPItype = "";
	BPIElement.name = "";	 
}

function create_map( one ) {
	var funcBody = one.toString();
	var strelement = funcBody.split( funcBody.split( "\n" ) ); 
	var elements = [];
	alert( funcBody );
}

create_map( БизнесПроцесс );